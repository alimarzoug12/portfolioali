{
  "buildCommand": "cd frontend && npm install && npm run build && cd ../portfolio && npm install && DISABLE_ESLINT_PLUGIN=true CI=false npm run build && cp -r portfolio/build frontend/build/portfolio",
  "outputDirectory": "frontend/build",
  "installCommand": "npm install --legacy-peer-deps",
  "routes": [
    {
      "src": "/portfolio/(.*)",
      "dest": "/portfolio/index.html"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}