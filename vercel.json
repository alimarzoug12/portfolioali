{
  "buildCommand": "cd frontend && npm ci --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build && cd ../portfolio && npm ci --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build && cd .. && node copy-fixed.js",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Installation in build command'"
}