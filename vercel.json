{
  "buildCommand": "echo 'Testing portfolio build...' && cd portfolio && echo 'Installing...' && npm install && echo 'Building...' && DISABLE_ESLINT_PLUGIN=true npm run build && echo 'Build completed'",
  "outputDirectory": "frontend/build",
  "installCommand": "npm install --legacy-peer-deps"
}