{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "npm install --prefix frontend && npm install --prefix portfolio",
  "rewrites": [
    {
      "source": "/portfolio/:path*",
      "destination": "/portfolio/index.html"
    }
  ]
}