{
  "name": "portfolioali",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix frontend\" \"npm start --prefix portfolio\"",
    "build": "cd frontend && npm run build && cd ../portfolio && npm run build && cd .. && cp -r portfolio/build frontend/build/portfolio"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
