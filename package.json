{
  "name": "portfolioali-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Building both projects...'",
    "build:portfolio": "cd portfolio && npm install --legacy-peer-deps --no-audit --no-fund && CI=false DISABLE_ESLINT_PLUGIN=true npm run build",
    "build:frontend": "cd frontend && npm install --legacy-peer-deps --no-audit --no-fund && npm run build",
    "build:all": "npm run build:portfolio && npm run build:frontend && npm run copy:portfolio",
    "copy:portfolio": "node copy.js"
  }
}