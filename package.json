{
  "name": "portfolioali",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix frontend\" \"npm start --prefix portfolio\"",
    "build": "npm run build --prefix frontend && npm run build --prefix portfolio && cp -r portfolio/build frontend/build/portfolio"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
