{
  "name": "portfolioali-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Root package for Vercel deployment",
  "scripts": {
    "vercel-build": "cd frontend && npm run build && cd ../portfolio && CI=false npm run build && cd .. && cp -r portfolio/build frontend/build/portfolio"
  }
}